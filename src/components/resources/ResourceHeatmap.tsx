const ResourceHeatmap = ({ deptFilter }: { deptFilter: string }) => {
  return (
    <div className="text-center py-12 text-muted-foreground">
      <p className="text-sm">Mapa de capacidade disponível na listagem de recursos.</p>
    </div>
  );
};

export default ResourceHeatmap;
